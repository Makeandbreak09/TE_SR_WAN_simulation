These are the results after running the programm for 100h.
They only contain the LLLF as the extra algorithm, because the other were not ready. 
Also the Algorithm only stored the MLU in the json file.
